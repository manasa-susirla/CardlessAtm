<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <Preference android:title="Add Account">
       />
    </Preference>
    <Preference android:title="Change Screen Pin">
        />
    </Preference>

    <Preference android:title="Sign Out">
        <intent android:action="com.example.lenovo.cardlessatm.SignOutActivity"
        />
    </Preference>
    <Preference android:title="View Nearby ATM's">
        />
    </Preference>

</PreferenceScreen>
